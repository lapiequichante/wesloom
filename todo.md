Future things worth doing before a release:
- remove serde-json and build own proprietary format ? json is cool as long as we can compile it before release and not embedded this in production code
- make sure we can compile every shader before going to production (link with minimal libs)
- make sure it runs on the web (webgl2)

