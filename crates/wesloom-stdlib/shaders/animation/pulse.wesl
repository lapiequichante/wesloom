// A 0..1 sine oscillation: `frequency` cycles per second, `phase` in turns.
fn pulse(time: f32, frequency: f32, phase: f32) -> f32 {
    return sin((time * frequency + phase) * 6.28318531) * 0.5 + 0.5;
}
