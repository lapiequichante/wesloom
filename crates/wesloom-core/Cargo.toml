[package]
name = "wesloom-core"
description = "GUI-agnostic shader node graph model, node/socket traits and WESL codegen glue for wesloom. No wgpu, no GUI toolkit."
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
license.workspace = true

[dependencies]

[lints.rust]
unsafe_code = "forbid"
