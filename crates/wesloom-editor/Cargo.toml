[package]
name = "wesloom-editor"
description = "Optional visual node editor for wesloom graphs. Pulls in a GUI toolkit; never a dependency of wesloom-core or wesloom-render."
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
license.workspace = true

[dependencies]
wesloom-core = { workspace = true }
# egui = "..."  # added once the editor UI work starts; see docs/adr/0004.
